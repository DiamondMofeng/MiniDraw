按照自己的思路一直写下去，结果与前期计划的有些不同。  
Canvas组件最后仅成为了canvas dom对象的载体。  
各绘画对象也没有分离出来，而是直接写在了pen里。   
Pen直接变成了对canvas的操作类。。

各小功能实现
---
* 撤销  
  每次绘图操作完成后，将当前画布保存并压入栈。  
  进行撤销操作时，恢复画布为栈顶第二个画布对象。  
  最后将栈顶元素弹出。

* 清空画布  
  记得清空撤销栈。

* 预览  
  每次绘图开始时，保存当前画布为临时画布。  
  开始绘制后，检测到鼠标移动时，清空画布并读取临时画布，然后绘制当前预览结果。  
